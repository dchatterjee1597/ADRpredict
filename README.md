# Predictive modelling of drug side effects (ADR) using bioinformatics + ML

Goal: predict whether a drug is associated with a given adverse drug reaction (ADR).
Week 2 focus: **raw data ingestion + provenance + catalog + validation** (reproducible).

---

## Quick start (Windows + VS Code)

### 0) Create + activate a venv (recommended)
PowerShell:
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
